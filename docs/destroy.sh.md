Destroys a virtual machine. Once the virtual machine is destroyed, it is removed from the `vm_list` parameter.

If `VPN_NODE` is defined in your `config.conf`, the virtual machine will be removed from the VPN.
